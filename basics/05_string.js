const otherName='mejbha';
const myAge=34;

// console.log(`my name is ${otherName} and my age is${myAge}`);

const myName=new String('mejbha');
console.log(myName.indexOf('b'));

// string and newString are not same have huge difference one is primitive string and other is string object.